

const SectionTitle = ({ title, heading }) => {
    return (
        <div>
            <h4 className="font-2xl ">{heading}</h4>
            <h2 className="font-2xl ">{title}</h2>
        </div>
    );
};

export default SectionTitle;